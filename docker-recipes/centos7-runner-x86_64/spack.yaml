 spack:
  view: /opt/view
  concretization: separately

  config:
    install_tree: /opt/software

  packages:
    all:
      target: [x86_64]

  specs:
  - gcc@8.1.0+strip

  mirrors:
    prl: https://cache.e4s.io
