 spack:
  view: /opt/view
  concretization: separately

  config:
    install_tree: /opt/software

  packages:
    all:
      target: [ppc64le]

  specs:
  - gcc@8.1.0+strip

  mirrors:
    E4S: https://cache.e4s.io
