 spack:
  view: /bootstrap/runner/view
  concretization: separately

  config:
    install_tree:
      root: /bootstrap/runner/install

  packages:
    all:
      target: [x86_64]

  specs:
  - patchelf
