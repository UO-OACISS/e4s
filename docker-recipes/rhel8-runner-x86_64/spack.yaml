 spack:
  view: /opt/view
  concretization: separately

  config:
    install_tree: /opt/software

  packages:
    all:
      target: [x86_64]

  specs: []

  mirrors:
    prl: https://cache.e4s.io/e4s
