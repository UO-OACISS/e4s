spack:
  view: /bootstrap/runner/view
  
  config:
    install_tree:
      root: /bootstrap/runner/install
  
  concretizer:
    reuse: false
    unify: false

  packages:
    all:
      compiler: [gcc]
      target: [ppc64le]

  specs:
  - libtree
  - lmod
  - patchelf
