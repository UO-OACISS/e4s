 spack:
  view: /bootstrap/runner/view
  
  config:
    install_tree:
      root: /bootstrap/runner/install

  concretizer:
    reuse: false
    unify: false

  packages:
    all:
      compiler: [gcc]
      target: [x86_64]

  specs:
  - libtree
  - lmod
  - patchelf
